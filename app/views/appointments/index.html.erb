<%= link_to 'Novo horário', new_appointment_path %>

<% if @appointments.empty? %>
  <div id = "appointments_list">
    Nenhuma atividade agendada
  </div>
<% end %>

<div>
<% @appointments.each do | appointment | %>
  <%= link_to l(appointment.appointment_date, format: :default), appointment %></br>
<% end %>
</div>

