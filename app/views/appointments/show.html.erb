<%= link_to 'Editar', edit_appointment_path(@appointment) if current_personal == @appointment.personal %>

<%= I18n.l(@appointment.appointment_date, format: :default) %>
<%= number_to_currency(@appointment.price_per_hour) %>
