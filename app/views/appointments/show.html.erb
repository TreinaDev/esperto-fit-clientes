<%= link_to 'Editar', edit_appointment_path(@appointment) if @appointment.owner?(current_personal) %>

<% if client_signed_in? %>
  <%= link_to 'Agendar este horário', appointment_order_appointments_path(@appointment), method: :post %>
<% end %>

<%= l(@appointment.appointment_date, format: :default) %>
<%= number_to_currency(@appointment.price_per_hour) %>

<% if @appointment.ordered? %>
  <%= link_to 'Cancelar agendamento', cancel_appointment_path(@appointment), method: :put if @appointment.owner?(current_personal) %>
<% end %>
