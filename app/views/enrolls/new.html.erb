<%= form_with model: @enroll, url: subsidiary_enrolls_path, local:true do |f| %>
    <%= f.label 'Selecione seu plano' %>
    <%= f.collection_select('Selecione seu plano', @subsidiary.plans, :id, :description) %>
    <%= f.label 'Forma de pagamento' %>
    <%= f.collection_select('Forma de pagamento', @payment_options, :id, :name) %>
    <%= f.submit 'Próximo' %>
<% end %>